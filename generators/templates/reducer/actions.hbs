import { createAction } from '@reduxjs/toolkit';

import { HelloWorldPayload } from 'types/requests';

import actionTypes from './actionTypes';

export const helloWorld = createAction<HelloWorldPayload>(actionTypes.HELLO_WORLD);
