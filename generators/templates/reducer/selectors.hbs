import type { State, {{ properCase reducerName }}State } from 'types';

const {{ camelCase reducerName }}Selectors =  {
  get{{ properCase reducerName }}: (state: State): {{ properCase reducerName }}State => state.{{ camelCase reducerName }},
  getProp: (propKey: keyof {{ properCase reducerName }}State) => (state: State) => state.{{ camelCase reducerName }}[propKey],
};

export default {{ camelCase reducerName }}Selectors;
