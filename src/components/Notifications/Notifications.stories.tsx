import { Notifications } from './Notifications';

export default {
  title: 'components/Notifications',
  component: Notifications,
};

export const Default = () => <Notifications notifications={[]} />;
