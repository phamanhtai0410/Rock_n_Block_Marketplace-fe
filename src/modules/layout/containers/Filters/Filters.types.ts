export interface ICategory {
  id: number | string;
  title: string;
  icon: string;
}

export interface ICollection {
  value: string;
  title: string;
  network: string;
}

export interface ICurrency {
  value: string;
  title: string;
  network: string;
}
